# 포트폴리오 웹사이트 문구 개편안

## 1. 현재 포트폴리오 웹사이트 텍스트의 문제점 진단

- Hero의 "안전한 혁신", "Security-minded", "Risk Management"는 방향성은 있지만 첫 10초에 프론트엔드 문제 해결력이 바로 보이지 않습니다. 당근/오늘의집형 회사에서는 생활 서비스·탐색 흐름보다 보안/시스템 이미지가 먼저 읽힐 수 있습니다.
- 첫 화면 수치 중 `0 plain text`는 보안 프로젝트 증거로는 좋지만, 프로덕트 프론트엔드 채용에서는 `80K logs/sec`, `1M rows`, 렌더링/메모리 개선보다 우선순위가 낮습니다.
- Selected Work에 학부/사이드 프로젝트가 먼저 보이고, 실무 핵심 프로젝트(Log Analyzer, 100만 행 Export, DDD 리팩터링, 오픈소스)가 상세 카드로 덜 드러납니다. 토스/쿠팡형 면접관에게는 가장 강한 증거가 뒤에 숨어 보입니다.
- 기술 스택 섹션은 기술명이 잘 정리되어 있지만, "이 기술로 어떤 사용자 문제와 병목을 해결했는지"가 약합니다.
- 프로젝트 상세는 Overview/Why/Role/Result 구조가 있으나 "제약 조건", "제외한 선택지", "다음 프로젝트에서 달라진 판단 기준"이 분리되어 있지 않아 실무자의 후속 질문을 선점하지 못합니다.

줄이거나 바꿀 표현:

- "성장하는 개발자", "열정적인 개발자", "사용자 중심"처럼 증거 없이 쓰는 표현
- "안전한 혁신", "심리적 안전지대", "팀의 이익"처럼 가치관만 먼저 나오는 표현
- "프로젝트 보기", "글 읽기"처럼 generic한 버튼
- "C, Linux를 학습하기 위해"처럼 학습 목적만 강조하는 프로젝트 설명

## 2. Hero 섹션 문구 후보 5개

짧은 버전:

> 복잡한 데이터 흐름을 사용자가 헤매지 않는 인터페이스로 정리합니다.

강한 버전:

> 성능 병목과 상태 구조를 뜯어고쳐, 분석 흐름이 멈추지 않는 프론트엔드를 만듭니다.

프로덕트 기업용 버전:

> 사용자 작업 흐름과 기술적 병목 사이에서 문제를 찾고, 프론트엔드로 해결합니다.

성능/대용량 데이터 강조 버전:

> 초당 80,000건 로그와 100만 행 Export를 다루며, 대용량 데이터에서도 멈추지 않는 UI를 설계했습니다.

사용자 경험 강조 버전:

> 복잡한 분석 과정을 사용자가 다음 판단으로 이어갈 수 있는 화면 흐름으로 바꿉니다.

추천 첫 화면 조합:

> 사용자 작업 흐름과 기술적 병목 사이에서 문제를 찾고, 프론트엔드로 해결합니다.  
> 초당 80,000건 로그 처리, 100만 행 Export, 상태 구조 재설계를 경험했습니다.

태그:

`Frontend Engineer` `Performance UI` `State Architecture` `Large Data UX`

## 3. About Me 최종안

500자 이내:

> 저는 복잡한 데이터 흐름과 사용자 작업 흐름이 만나는 지점에서 문제를 찾는 프론트엔드 개발자입니다. 차량 로그 분석 도구에서 초당 80,000건 로그를 UI 멈춤 없이 시각화했고, 100만 행 CSV Export를 3초 안에 처리하며 메모리 사용량을 60% 줄였습니다. 단순히 빠르게 만드는 것보다, 사용자가 분석 맥락을 잃지 않고 다음 판단으로 넘어가게 만드는 구조를 중요하게 봅니다. Global State를 Local State와 도메인 단위 구조로 재정리해 리렌더링을 줄였고, 반복되는 로직은 npm 패키지와 테스트 가능한 모듈로 분리했습니다.

300자 이내:

> 복잡한 데이터 흐름을 사용자가 판단하기 쉬운 인터페이스로 정리하는 프론트엔드 개발자입니다. 초당 80,000건 로그 시각화, 100만 행 Export 최적화, Global State 재구조화를 경험했습니다. 성능 수치뿐 아니라 다음 개발자가 이해할 수 있는 상태 구조와 사용자가 헤매지 않는 작업 흐름을 함께 봅니다.

아주 짧은 2문장:

> 복잡한 데이터 흐름과 사용자 작업 흐름 사이에서 병목을 찾고, 프론트엔드 구조로 해결합니다. 대용량 로그 처리, Export 최적화, 상태 구조 재설계를 통해 멈추지 않는 분석 경험을 만들어 왔습니다.

## 4. 기술 스택 섹션 최종안

섹션 제목:

> 문제를 해결하기 위해 선택한 기술

섹션 설명:

> 기술 이름보다 먼저 본 것은 병목의 위치였습니다. 렌더링이 문제인지, 상태 전파가 문제인지, 메인 스레드가 막히는지에 따라 구조를 다르게 선택했습니다.

Frontend Core:

- React / TypeScript: 복잡한 화면 상태와 데이터 타입을 명확히 분리해, 분석 UI의 변경 범위를 줄입니다.
- Zustand / Local State: 모든 상태를 전역으로 올리지 않고, 영향 범위가 필요한 곳에만 닿도록 상태 구조를 나눕니다.
- TailwindCSS: 상태별 UI를 빠르게 실험하되, 반복되는 스타일은 컴포넌트 규칙으로 고정합니다.

Large Data UI:

- Web Worker: 대용량 리스트·스크롤 연산을 메인 스레드에서 분리해 입력과 탐색이 끊기지 않게 만듭니다.
- Virtual Scroll: 사용자가 보는 범위만 렌더링해 100만 건 규모의 데이터도 탐색 가능한 UI로 다룹니다.
- Stream API: Export 데이터를 한 번에 메모리에 올리지 않고 흐름 단위로 처리해 브라우저 메모리 리스크를 줄입니다.

Architecture:

- DDD / Domain Slice: 기능 단위가 아니라 도메인 단위로 책임을 나눠 다음 개발자가 변경 지점을 찾기 쉽게 만듭니다.
- Event Bus / IPC: Electron 환경에서 수집·분석·화면 갱신 흐름을 분리해 실시간 데이터 동기화를 안정화합니다.
- Custom Hooks: 반복되는 비동기·상태 로직을 화면 밖으로 빼서 기능 추가 시 회귀 위험을 줄입니다.

Quality & Reuse:

- Vitest / GitHub Actions: 공개 가능한 품질의 프론트엔드 모듈을 만들기 위해 테스트와 배포 흐름을 자동화했습니다.
- npm Package: 업무에서 검증한 원형 큐와 이벤트 전달 구조를 재사용 가능한 패키지로 분리했습니다.

## 5. 프로젝트 카드 문구 최종안

### 실시간 차량 로그 Monitor & Analyzer

- 한 줄 요약: 초당 80,000건 차량 로그를 UI 멈춤 없이 시각화한 Electron 기반 분석 도구
- 핵심 수치: 80K logs/sec, 렌더링 2.3배 개선, 메모리 30% 절감
- 사용 기술: React, TypeScript, Electron, ZeroMQ, Zustand, DevExtreme
- 짧은 설명: 외부 라이브러리 병목을 프로파일링하고 상태·렌더링 흐름을 재설계해, 평가자가 분석 흐름을 끊지 않도록 만들었습니다.

### 100만 행 DLT Export 최적화

- 한 줄 요약: 100만 행 로그를 3초 안에 Export하면서 메모리 사용량을 줄인 대용량 데이터 처리 프로젝트
- 핵심 수치: 1M rows, 3초 Export, 메모리 60% 절감
- 사용 기술: React, Electron, Stream API, DevExtreme DataGrid, rc-dock, IPC
- 짧은 설명: 파일 생성 전체를 메모리에 쌓지 않고 스트림으로 처리해, 대용량 분석 작업의 대기와 실패 리스크를 줄였습니다.

### VTDM 상태 구조/DDD 리팩터링

- 한 줄 요약: Global State 중심 레거시를 도메인 단위 구조로 재정리한 프론트엔드 아키텍처 개선
- 핵심 수치: 리렌더링 80% 감소, 코드량 30% 절감
- 사용 기술: React, TypeScript, DDD, Web Worker, AgGrid, Custom Hooks
- 짧은 설명: 단순 기능 추가보다 변경 영향 범위를 줄이는 구조를 우선해, 다음 개발자가 이해하기 쉬운 화면 상태 흐름을 만들었습니다.

### 오픈소스 프론트엔드 라이브러리 2종

- 한 줄 요약: 실무에서 검증한 이벤트 전달·원형 큐 로직을 npm 패키지로 분리
- 핵심 수치: npm 2종 배포, Vitest 커버리지 100%, GitHub Actions CI/CD
- 사용 기술: TypeScript, React Hooks, Vitest, GitHub Actions, npm
- 짧은 설명: 사내 유틸에 머물 수 있던 로직을 설치 가능한 API와 테스트 가능한 구조로 정리했습니다.

### CARLA × Alpamayo 시뮬레이션 검증 환경

- 한 줄 요약: 자율주행 궤적 예측 모델을 실시간 시뮬레이션 루프에 연결한 검증·시각화 환경
- 핵심 수치: 추론 지연 1.2s → 0.78s, FPS 18 → 28
- 사용 기술: Python, C++, CARLA, PyTorch, Hugging Face, Pandas
- 짧은 설명: 학습 지표가 실제 운용 흐름에서도 의미 있는지 확인하기 위해, 추론·시각화·평가 로깅을 한 흐름으로 묶었습니다.

### 기존 Selected Work 보조 카드 문구

- P2P Service: `fork`와 `flock`으로 동시 접속 파일 서버의 무결성을 검증한 시스템 프로그래밍 프로젝트
- Secure Chat: RSA/AES 역할 분리로 패킷 캡처 시 평문 노출을 막은 Java Socket 채팅
- Vary Recycle: 촬영-인식-가이드-리워드를 한 흐름으로 연결한 분리수거 리워드 앱
- 리뷰 왓수다: 20만+ 리뷰 데이터를 전처리하고 LSTM/BERT 감성 분석을 비교한 NLP 프로젝트
- 모강: 1000+ 강의 데이터를 탐색·비교할 수 있게 만든 Next.js 기반 강의 추천 서비스
- 쌍상: 100+ 회원의 봉사 신청·관리 흐름을 웹앱으로 정리한 운영 자동화 프로젝트
- 서울지금: 현재 레포 데이터에서 확인하지 못했습니다. 프로젝트 목적, 사용자 수, 본인 역할, 핵심 수치 [본인 보강 필요]

## 6. 프로젝트 상세 페이지 문구 최종안

### 6-1. 실시간 차량 로그 Monitor & Analyzer

한 줄 요약:

> 초당 80,000건 차량 로그를 UI 멈춤 없이 시각화하고, 분석자가 다음 판단으로 바로 넘어갈 수 있게 만든 실시간 로그 분석 도구입니다.

문제 상황:

> 10분 이상 실시간 로그를 수신하면 메모리와 CPU 사용량이 급증했고, 평가자는 화면이 멈추는 동안 분석 맥락을 잃었습니다. 단순히 데이터를 많이 받는 문제가 아니라, 수집·상태 갱신·렌더링이 한 번에 얽힌 작업 흐름 문제였습니다.

제약 조건:

> 외부 UI 라이브러리 내부 동작을 바꿀 수 없었고, 차량 로그는 실시간으로 계속 유입되었습니다. Electron 환경에서 메모리 한계를 고려해야 했으며, 분석자가 사용 중인 화면 흐름을 크게 바꾸지 않는 선에서 개선해야 했습니다.

기술적 판단:

> 먼저 기능을 덜어내기보다 프로파일러로 병목 위치를 확인했습니다. 라이브러리를 교체하면 일정과 회귀 리스크가 커진다고 판단해, 인스턴스 제어와 렌더링 흐름 최적화로 우회했습니다. 상태 관리는 모든 데이터를 전역에 올리지 않고, 실시간 동기화가 필요한 데이터와 화면 지역 상태를 분리했습니다.

구현 내용:

- ZeroMQ/IPC 기반 비동기 수집 파이프라인 구성
- DevExtreme CustomStore와 가상 스크롤 기반 대용량 탐색 최적화
- Zustand와 Custom Store로 실시간 데이터 동기화 구조 분리
- 프로파일링을 통해 Validation Controller 캐싱 문제를 추적하고 인스턴스 제어
- 평가 엔지니어 피드백을 반영해 분석 화면 흐름 재정리

결과:

> 초당 80,000건 이상의 로그를 UI Freezing 없이 시각화했고, 렌더링은 2.3배 개선했습니다. 메모리 사용량은 3.3GB에서 2.0GB로 줄였고, 반복 분석 시간은 5분에서 30초 수준으로 단축했습니다.

배운 점:

> 프론트엔드 성능 개선은 숫자를 줄이는 일이 아니라 사용자의 판단 흐름을 지키는 일이라는 기준이 생겼습니다. 이후에는 라이브러리 교체보다 먼저 실제 병목과 변경 비용을 함께 봅니다.

면접 예상 질문:

1. 초당 80,000건 처리량은 어떤 환경과 기준으로 측정했나요?
2. DevExtreme을 유지한 이유와 교체하지 않은 판단 근거는 무엇인가요?
3. 메모리 누수 원인을 어떻게 재현하고 검증했나요?
4. Zustand와 Local State의 경계를 어떻게 나눴나요?
5. 실시간 데이터가 많아질 때 UI Freezing을 막기 위한 다음 개선안은 무엇인가요?

### 6-2. 100만 행 DLT Export 최적화

한 줄 요약:

> 100만 행 로그를 3초 안에 Export하면서 메모리 사용량을 줄이고, 사용자의 분석 흐름을 끊지 않게 만든 대용량 Export 최적화 프로젝트입니다.

문제 상황:

> 사용자는 필터링한 로그를 반복적으로 내보내야 했지만, 대용량 CSV 생성 과정에서 메모리 폭증과 대기 시간이 발생할 수 있었습니다. Export 실패는 단순 기능 실패가 아니라 분석 작업 전체를 다시 시작하게 만드는 병목이었습니다.

제약 조건:

> 브라우저/Electron 메모리 한계 안에서 100만 행 이상을 처리해야 했고, 사용자는 필터링·패널 배치·외부 뷰어 확인을 반복했습니다. 기존 DataGrid 사용 흐름은 유지해야 했습니다.

기술적 판단:

> 전체 데이터를 문자열로 만든 뒤 파일로 저장하는 방식은 메모리 리스크가 크다고 판단했습니다. Stream API를 사용해 데이터를 흐름 단위로 처리하고, 화면 탐색은 가상 스크롤과 도킹 레이아웃으로 분리했습니다. 단순 Export 기능보다 반복 분석 워크플로우의 손실을 줄이는 쪽을 우선했습니다.

구현 내용:

- Stream API 기반 CSV Export 파이프라인 구현
- DevExtreme DataGrid 가상 스크롤 고도화
- rc-dock 기반 패널 배치 저장·복원
- IPC 기반 외부 뷰어 원클릭 연동
- 정규식 호환성과 렌더링 이슈를 트러블슈팅 문서로 정리

결과:

> 100만 행 CSV Export를 3초 이내에 처리했고, Export 메모리 사용량을 60% 줄였습니다. 100만 건 로그 로딩·탐색은 평균 2초 이내로 맞췄으며, 패널 저장과 외부 뷰어 연동으로 반복 작업 흐름을 줄였습니다.

배운 점:

> 대용량 처리는 "빠르게 끝나는가"와 함께 "실패했을 때 사용자가 어디까지 잃는가"를 봐야 합니다. 이후에는 성능 개선을 사용자 작업 단위와 함께 측정하려고 합니다.

면접 예상 질문:

1. Stream API를 적용하기 전 방식의 병목은 무엇이었나요?
2. 100만 행 Export 3초는 어떤 파일 크기와 환경에서 측정했나요?
3. Export 도중 취소·에러·부분 실패는 어떻게 처리했나요?
4. rc-dock을 도입한 이유와 대안은 무엇이었나요?
5. 브라우저 메모리 한계가 더 낮은 환경이면 어떻게 대응하겠나요?

### 6-3. VTDM 상태 구조/DDD 리팩터링

한 줄 요약:

> 전역 상태에 얽힌 레거시 화면을 도메인 단위로 재정리해, 리렌더링과 변경 영향 범위를 줄인 아키텍처 개선 프로젝트입니다.

문제 상황:

> 기능은 동작했지만 Global State에 많은 흐름이 얽혀 있어 작은 변경도 예상 밖의 리렌더링과 회귀를 만들었습니다. 새 기능을 붙일수록 다음 개발자가 변경 지점을 찾기 어려운 구조였습니다.

제약 조건:

> 운영 중인 시스템이라 한 번에 갈아엎을 수 없었고, 기능 추가 일정도 함께 존재했습니다. 기존 화면과 API 흐름을 유지하면서 점진적으로 구조를 나눠야 했습니다.

기술적 판단:

> 전역 상태를 더 정교하게 관리하기보다, 전역일 필요가 없는 상태를 먼저 지역화했습니다. 기능 기준 폴더링만으로는 변경 이유가 보이지 않아 도메인 단위 경계를 적용했고, 무한 스크롤 연산은 Web Worker로 분리했습니다.

구현 내용:

- Global State를 Local State와 도메인 상태로 분리
- DDD 기반 모듈 경계 재정리
- 반복 로직을 Custom Hooks로 추상화
- Web Worker로 무한 스크롤 DOM 연산 분산
- AgGrid/AgChart 커스터마이징으로 대시보드 UI 구성

결과:

> 리렌더링을 5회에서 1회로 줄여 80% 감소시켰고, 코드량은 30% 줄였습니다. 상태 변경 영향 범위가 줄어 기능 추가와 Hotfix 시 확인해야 할 영역이 명확해졌습니다.

배운 점:

> 구조 개선은 멋진 패턴을 도입하는 일이 아니라, 다음 변경을 덜 위험하게 만드는 일이어야 합니다. 이후에는 "이 상태가 정말 전역이어야 하나?"를 먼저 묻습니다.

면접 예상 질문:

1. DDD를 프론트엔드에 적용할 때 도메인 경계는 어떻게 잡았나요?
2. Global State를 Local State로 내리면서 생긴 단점은 없었나요?
3. 리렌더링 80% 감소는 어떤 기준으로 측정했나요?
4. 일정 압박 속에서 리팩터링 범위를 어떻게 설득했나요?
5. 같은 상황이라면 Zustand, Context, 서버 상태 관리를 어떻게 나누겠나요?

### 6-4. 오픈소스 프론트엔드 라이브러리 2종

한 줄 요약:

> 실무에서 반복 사용한 원형 큐와 이벤트 전달 구조를 설치 가능한 npm 패키지로 분리한 재사용 자산화 프로젝트입니다.

문제 상황:

> 로그·스트리밍 UI에서 최신 값을 유지하는 자료구조와 Prop Drilling 없이 이벤트를 전달하는 구조가 반복되었습니다. 사내 유틸로만 두면 다른 프로젝트에서 재사용하기 어렵고, API 품질도 검증하기 어려웠습니다.

제약 조건:

> 공개 패키지로 배포하려면 타입 안정성, 문서, 테스트, 배포 자동화가 필요했습니다. React에 묶인 훅은 제공하되, 핵심 로직은 React 없이도 사용할 수 있어야 했습니다.

기술적 판단:

> 업무 코드 전체를 공개하는 대신, 도메인 의존성을 제거할 수 있는 순수 로직만 패키지화했습니다. "설치했을 때 바로 이해되는 API"가 중요하다고 보고 class/core module과 React hook 계층을 나눴습니다.

구현 내용:

- `circular-queue-react` 자료구조 로직과 React hook 분리
- `broadcast-event-system` 타입 안전 이벤트 버스 구현
- Vitest 기반 단위 테스트 작성
- GitHub Actions 기반 CI/CD 구성
- npm 배포와 README 사용 예시 정리

결과:

> npm 패키지 2종을 배포했고, Vitest 커버리지 100%를 맞췄습니다. 다운로드 수, 실제 외부 사용 사례, 사내 코드 감소량은 [본인 보강 필요]입니다.

배운 점:

> 라이브러리의 품질은 내부 구현보다 사용자가 처음 마주하는 API와 실패 메시지에서 결정된다는 점을 배웠습니다. 이후 공통 로직을 만들 때 "다른 사람이 설치해도 맥락 없이 쓸 수 있는가"를 기준으로 봅니다.

면접 예상 질문:

1. 두 패키지를 만들게 된 실무 문제는 무엇이었나요?
2. React hook과 core logic을 분리한 이유는 무엇인가요?
3. Vitest 커버리지 100%에서 어떤 케이스를 가장 중요하게 봤나요?
4. npm 배포 후 버전 관리와 breaking change는 어떻게 관리했나요?
5. 기존 이벤트 버스 라이브러리 대신 직접 만든 이유는 무엇인가요?

### 6-5. CARLA × Alpamayo 시뮬레이션 검증 환경

한 줄 요약:

> 자율주행 궤적 예측 모델을 실시간 시뮬레이션 루프에 연결하고, 추론·시각화·평가 로깅을 반복 가능한 검증 흐름으로 만든 프로젝트입니다.

문제 상황:

> 학습 지표가 좋아도 실시간 시뮬레이션에서는 Tick, 추론 지연, 시각화 부하 때문에 다른 결과가 나올 수 있었습니다. 모델 결과를 보여주는 것보다 실제 운용 흐름에서 검증 가능한 구조가 필요했습니다.

제약 조건:

> CARLA 시뮬레이터 Tick과 모델 추론 속도를 맞춰야 했고, 다중 궤적을 시각화하면서도 프레임 드랍을 줄여야 했습니다. 진행형 프로젝트이므로 운영 적용 결과는 [본인 보강 필요]입니다.

기술적 판단:

> 학습 코드와 시뮬레이션 UI를 단순 연결하지 않고, 동기/비동기 추론 파이프라인을 나눴습니다. 정확도는 최종 수치만 보지 않고 Point-wise 좌표 오차와 ADE/FDE/RMSE로 검증했습니다.

구현 내용:

- CARLA 소스 커스터마이징 및 모델 양방향 통신 구조 설계
- 차량 Tick 기반 추론 제어 로직 구현
- Point-wise 좌표 오차, ADE/FDE/RMSE 산출 모듈 구현
- 다중 궤적 실시간 시각화 최적화
- 시나리오별 오차율·추론 시간·FPS CSV 로깅 자동화

결과:

> 현재 사이트 데이터 기준 추론 지연을 1.2초에서 0.78초로 줄였고, FPS는 18에서 28로 개선했습니다. ADE 0.72m, FDE 1.84m, RMSE 0.91m 기준의 검증 체계를 구축했습니다. 운영 반영 범위와 팀 기여도는 [본인 보강 필요]입니다.

배운 점:

> 모델의 가치는 학습 결과가 아니라 실제 운용 흐름에서 결정된다는 기준이 생겼습니다. 프론트엔드에서도 지표를 보여주는 화면보다, 지표가 재현되고 검증되는 흐름을 더 중요하게 보게 됐습니다.

면접 예상 질문:

1. 동기/비동기 추론 파이프라인을 어떻게 나눴나요?
2. 0.78초 지연은 실시간 요구사항을 충족한다고 판단한 근거가 있나요?
3. ADE/FDE/RMSE 계산 모듈을 직접 구현한 이유는 무엇인가요?
4. 다중 궤적 시각화에서 병목은 CPU, GPU, 렌더링 중 어디였나요?
5. 이 경험이 프론트엔드 제품 개발과 어떻게 연결된다고 보나요?

## 7. 회사별 첫 화면 강조 문구

당근 지원용:

> 복잡한 흐름을 사용자가 헤매지 않는 화면으로 줄입니다. 대용량 로그 분석 도구에서 성능 병목을 고치면서도, 평가자가 다음 판단으로 넘어가는 흐름을 먼저 봤습니다.

토스 지원용:

> 성능 병목을 프로파일링하고, 반복되는 프론트엔드 구조는 재사용 가능한 자산으로 분리합니다. 렌더링 2.3배 개선, npm 패키지 배포, Vitest 100% 커버리지를 경험했습니다.

오늘의집 지원용:

> 사용자가 탐색과 비교 흐름을 끊지 않도록, 대용량 데이터를 읽기 쉬운 인터페이스로 정리합니다. 빠른 화면보다 사용자가 다음 행동을 자연스럽게 고를 수 있는 구조를 봅니다.

쿠팡 지원용:

> 대규모 데이터에서도 실패하지 않는 프론트엔드를 설계합니다. 100만 행 Export 3초 처리, 메모리 60% 절감, 리렌더링 80% 감소 경험이 있습니다.

## 8. 버튼/CTA 문구 제안

Hero CTA:

- 문제 해결 과정 보기
- 대용량 데이터 UI 사례 보기
- 성능 개선 기록 보기
- 상태 구조 개선 사례 보기

프로젝트 카드 CTA:

- 케이스 스터디 보기
- 병목을 어떻게 찾았는지 보기
- 기술적 판단 보기
- 결과와 측정 기준 보기

상세 페이지 CTA:

- GitHub에서 코드 보기
- README에서 사용법 보기
- npm 패키지 보기
- 성능 실험 보기
- 다음 프로젝트 보기

README/GitHub 링크 설명:

- GitHub: 문제 정의, 구현 과정, 성능 측정, 테스트 구성을 함께 남긴 저장소입니다.
- README: 이 프로젝트가 해결한 문제와 기술 선택 이유를 먼저 볼 수 있도록 정리했습니다.
- npm: 실무에서 반복된 프론트엔드 로직을 설치 가능한 패키지로 분리한 결과입니다.

## 9. 삭제해야 할 표현 목록

추상적 표현:

- 성장하는 개발자
- 끊임없이 노력하는 개발자
- 사용자 중심 개발자
- 열정적인 프론트엔드 개발자
- 커뮤니케이션을 중요하게 생각합니다

약해 보이는 표현:

- 공부하기 위해 만들었습니다
- 여러 기술을 사용해봤습니다
- 최선을 다했습니다
- 다양한 경험을 했습니다
- 맡은 역할을 성실히 수행했습니다

신입 포트폴리오에서 흔한 표현:

- React를 활용한 웹 서비스
- 반응형 UI를 구현했습니다
- CRUD 기능을 구현했습니다
- 로그인 기능을 구현했습니다
- 팀 프로젝트를 진행했습니다

실무자 관점에서 신뢰가 떨어지는 표현:

- 완벽한 구조
- 무조건 빠른 성능
- 모든 환경에서 안정적
- 100% 보장
- 제가 전부 해결했습니다

현재 사이트에서 줄이면 좋은 표현:

- 안전한 혁신: 증거와 같이 쓰지 않으면 추상적으로 보입니다.
- Team Safety: 채용 첫 화면에서는 직무 역량보다 약하게 읽힙니다.
- Security-minded: 보안 회사가 아니라면 Hero 태그 우선순위를 낮추는 편이 좋습니다.
- Selected projects, measurable outcomes: 좋은 문장이지만, 실제 첫 카드가 핵심 실무 프로젝트가 아니면 약해집니다.

## 10. 최종 적용 순서

1. Hero를 먼저 고칩니다. 첫 문장을 "사용자 작업 흐름과 기술적 병목 사이에서 문제를 찾고, 프론트엔드로 해결합니다."로 바꾸고, 첫 화면 수치는 `80K logs/sec`, `1M rows`, `80% rerender reduction` 또는 `60% memory reduction`으로 재배치합니다.
2. Selected Work의 노출 순서를 바꿉니다. 실시간 로그 분석기, 100만 행 Export, VTDM 리팩터링, 오픈소스 라이브러리를 상단 대표 프로젝트로 올리고, 학부/사이드 프로젝트는 보조 섹션으로 내립니다.
3. About Me를 짧게 줄입니다. 사이버보안 전공 설명보다 "복잡한 데이터 흐름과 사용자 작업 흐름 사이의 병목 해결"을 먼저 둡니다.
4. 기술 스택 섹션을 기술명 나열에서 문제 해결 그룹으로 바꿉니다. `Large Data UI`, `State Architecture`, `Quality & Reuse`처럼 면접관이 역량을 바로 분류할 수 있게 합니다.
5. 각 프로젝트 상세에 `문제 상황`, `제약 조건`, `기술적 판단`, `결과`, `배운 점`을 명시합니다. 특히 "왜 이 방식을 선택했고 무엇을 제외했는지"를 추가합니다.
6. 버튼 문구를 generic한 "프로젝트 보기"에서 "문제 해결 과정 보기", "성능 개선 기록 보기"처럼 클릭 이유가 보이는 문장으로 바꿉니다.
7. README/GitHub 링크 문구를 보강합니다. 단순 저장소 링크가 아니라, 실무자가 검증할 수 있는 코드·측정·테스트 흔적이라는 점을 드러냅니다.
8. 회사별 지원 시 Hero 문구만 바꿀 수 있도록 variant를 준비합니다. 당근은 작업 흐름, 토스는 성능/재사용성, 오늘의집은 탐색 UX, 쿠팡은 규모/안정성을 첫 문장에 둡니다.
